type Phone = text;
type Name = text;
type Entry = 
 record {
   desc: text;
   phone: Phone;
 };
type DataList = 
 service {
   insert: (Name, Entry) -> ();
   lookup: (Name) -> (opt Entry);
   whoami: () -> (text);
 };
service : () -> DataList
